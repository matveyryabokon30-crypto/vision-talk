name: Retired RC3 publisher
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'RC3 publisher retired. Use checked RC4 authentication release; never publish intermediate source commits.'
