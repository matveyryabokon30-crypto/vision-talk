# Pablicus — 1C: выполненные измерения и блокировка

11 сентября 2026 года. `PABLICUS-BLOCK01-1C-INTEGRATION-20260911`, revision `1.0`.

**SUBSTEP_1C_BLOCKED. Интеграция 1C не завершена. READY и ACCEPTED не присвоены.**

## Версии и параллельная работа

WORK_START_HEAD: `dc4a6031e3a7b5248882f88dee11d83ccd88fd60`.
CODE_SHA / TESTED_SHA: `f00f253ca2adfe7afc07e64cc3a1dc8f53c5692d`.
Это точный SHA попытки проверки, завершившейся failure, не успешного кандидата.

При финальном чтении обнаружен параллельный коммит `4c23c150b806f4ac012047308a7efd2e0b40a265` с дополнением владельца 01. Проверенное сравнение: четыре документальных файла, без изменений runtime, тестов и workflow. В нём прямо сохранены текущая постановка 1C и последовательность работ. Эти изменения не перезаписаны; документы передачи создаются поверх его дерева. Ранее подготовленный документальный коммит `d4b1261ed628b60494bbf2022613ac72f2ca6712` не направлен в ветку.

Фактический HANDOFF_HEAD указывается квитанцией после создания коммита. От TESTED_SHA до передачи ожидаются два документальных коммита и семь файлов: четыре параллельных документа и три собственных INTEGRATION_1C_*. Конечный diff подлежит проверке. Новый успешный CI от документального коммита не подразумевается.

До параллельной документации Исполнитель добавил 15 файлов тестов/CI в трёх коммитах. Ни один файл `pablicus/` или прежнего набора 1B не изменён. Принятые 1A/1B не повторяются; исторический START_HEAD 1B не является началом 1C. План Исполнителем не менялся, утверждённое параллельное дополнение сохранено.

## Что действительно выполнено

Прочитаны требуемые PABLICUS_CONTEXT, решение владельца, полный исходный утверждённый план, решения 1A/1B и отчёт R3; получены текущие исходники и тесты полного CI checkout. Неизвестная переписка не реконструировалась.

CI 34631535896 создал исходный снимок и повторил 31 сценарий 1B. Его success не является интеграционным PASS.

В CI 34632403344 настоящий локальный index.html открылся с HTTP 200. Через интерфейс и рабочий SDK вошёл искусственный A. Настоящий чат c1 открыл один список сообщений и рабочую базу IndexedDB; vault сообщил `state=empty`, `commits=0`, файлов 0. Это лишь наблюдение запуска пустого хранилища. Сохранность текста/IDs/файлов и reload ещё не проверены.

Прогон остановился перед Полотном: сетевой fixture не описывал `/rest/v1/rpc/get_message_actions`, возвращал 501, и повторный toast перекрывал кнопку. Это ошибка тестовой модели, не подтверждённый дефект приложения. Соответствующий ответ добавлен в расширенную обвязку, но дальнейший полный путь не прошёл проверку.

Последний CI 34634080287 запустил девять независимых браузерных процессов. **0 PASS / 0 FAIL / 9 ERROR / 0 TIMEOUT / 0 NOT_RUN.** Все завершились до поведенческих assertions: `Locator.fill: Timeout 8000ms exceeded`, `#email` существует, но не видим. Диагностический сбор дополнительно прервался на `ReferenceError: PablicusDebug is not defined`. Точная причина раннего запуска ещё не установлена. Это не ожидаемый отрицательный контроль и не доказанный дефект продукта. Все массивы проверок этих случаев пусты.

## Требование → модули → сценарий → результат → evidence

Пути ниже — внутри последнего CI ZIP, кроме явно указанного первого probe.

| Требование | Настоящие модули / сценарии | Результат | Evidence |
|---|---|---|---|
| Маршруты и экраны | index, SDK, app/controller/chat/Canvas/bots; probe, routes | Только исходное открытие login/chat/пустой БД. Полная цепочка не подтверждена | CI 34632403344: probe/results.json; browser/routes/result.json |
| 50 переходов, ресурсы, тихий список | DOM, handlers, observers, timers, subscriptions; quiet, lifecycle | ERROR до assertions; 50 переходов и числа до/после не измерены | browser/quiet/result.json; browser/lifecycle/result.json |
| Текст, порядок, IDs, исходные байты, reload и leave/open | chat/composer/vault/rich-store/IndexedDB/Canvas; durability, canvas | ERROR до создания проверяемого черновика; сверки байтов нет | browser/durability/result.json; browser/canvas/result.json |
| Outbox, offline, потерянный ACK и retry | outbox/transport/rich-store/SDK; outbox | Нужные действия не достигнуты | browser/outbox/result.json |
| A → B → A, поздние чтение/запись/отправка и refresh | SDK Auth, app/chat/vault/outbox; isolation, pending-send | ERROR до assertions; полная изоляция не квалифицирована | browser/isolation/result.json; browser/pending-send/result.json |
| Отказы записи/загрузки и повтор | native IndexedDB и рабочие хранилища; storage-errors | ERROR до инъекции отказов | browser/storage-errors/result.json |
| Положительные/отрицательные контроли | мутации изолированных копий для цикла/изоляции/байтов | Не реализованы и не выполнены, NOT_RUN | Нет результата |

Все семь требований 1C остаются незакрытыми. Исходный FAIL приложения и исправленный PASS не получены. Подтверждённых исправлений приложения нет. Свежие **31/31 PASS 1B** относятся только к принятой регрессионной основе.

## Точная техническая блокировка

Следующая запись исправления тестового измерительного слоя вызовом `GitHub.create_tree` для `tests/engineering/block-01/integration_1c/instrument.js` остановлена сервисом инструментов OpenAI:

> Этот вызов инструмента был заблокирован OpenAI, поскольку мы не смогли определить статус безопасности запроса.

Отказ наблюдался не позднее `2026-09-11T18:43:20Z`; точное время самого вызова отдельно не записано. GitHub HTTP-код и SHA дерева не возвращены. Заблокированный вызов не продвинул ветку. Это не установленное отсутствие GitHub-доступа: чтения и отдельные документальные записи работают.

Эта правка не повторялась, не перепаковывалась и не направлялась другим инструментом для обхода защиты. Независимые чтения, проверка хешей и документация продолжены. Локальные незакоммиченные редакции сохранены отдельно, не выдаются за TESTED_SHA и не включаются в пакет как исполняемое исправление.

Минимальное внешнее действие — штатный разбор сервисом инструментов неопределённого статуса безопасности этой операции. Оснований менять права GitHub или просить новое продуктовое ТЗ нет. Возможность самостоятельно снять блокировку не установлена; отключение защиты не предлагается. Даже после разрешения операции остаётся исправить и проверить обвязку, исполнить сценарии и отрицательные контроли. Нельзя считать остальную работу законченной.

## Прежние препятствия и разрешённый транспорт

В 18:05:59Z `git ls-remote` дал `Could not resolve host: github.com`. Использованы работающие GitHub-коннектор и скачивание оригинальных артефактов; это препятствие обойдено разрешённым транспортом.

Локальный Chromium дал `ERR_BLOCKED_BY_ADMINISTRATOR`; HTTP-сервер отвечал 200. При чтении managed policy обнаружено `URLBlocklist=[*]`. Политики не менялись и не обходились; реальные прогоны выполнены в GitHub Actions.

`/mnt/data/1c-work/repo` — частичная копия исходников, не Git checkout. Изначального checkout в обследованном runtime не найдено. Рабочие копии других машин не обследовались и не перезаписывались. Незакоммиченные местные различия не потеряны и не распространяются как проверенные.

## CI и контрольные суммы

| Run | SHA | Artifact | Итог |
|---|---|---|---|
| 34631535896 | 23891c03bef4e7e293ed378c6e395e548df8e0ce | 10275674946 | success только захвата и 1B |
| 34632403344 | 60f8e0355f76f267448a9016140abb84890b8469 | 10277125856 | failure первоначального probe |
| 34634080287 | f00f253ca2adfe7afc07e64cc3a1dc8f53c5692d | 10277168648 | failure; 31 1B PASS и 9 браузерных ERROR |

Последний job: `103377640278`, attempt 1. URL:
https://github.com/matveyryabokon30-crypto/vision-talk/actions/runs/34634080287

Последний artifact: `996215` байт; срок до `2026-09-25T18:37:00Z`; оригинальная копия `PABLICUS_1C_BASELINE_MATRIX_34634080287.zip`.

ZIP SHA-256: `dedb3704e36ced0537f901ec65ce2d36f6cc1ba32330bd386320861ca2cc8d65`.

browser/results.json SHA-256: `0f782eca391cd27cf8ee0f8b1b8dfe2aa85aac50d2891cbdde4f730f2353427d`.

regression-1b.json SHA-256: `065fd3725892996a3388a95fc1a34142d1531cb91edd9b2e2629891b6731561e`.

SOURCE_MANIFEST.json SHA-256: `b8ac21dd7255e6dc4c86db1b261cdedb08c84eded7082d3bf5dc7e36f20592e9`.

Три оригинальных ZIP проверены по SHA-256 GitHub и CRC. Проверены 144 текущие копии исходников по SHA-256/Git blob и 18 хешей файлов результатов/логов браузерных случаев. Расхождений нет. Это целостность неуспешных доказательств, не успех приложения. Остальные ZIP, их сроки, размеры и хеши перечислены в манифесте.

Среда CI: Python 3.12.7, Node v22.23.2, ubuntu-24.04, Playwright 1.57.0. Первый probe использовал HeadlessChrome 143.0.7499.4. Локальная сверка артефактов не считается повторным браузерным запуском.

## Конечность, изоляция и завершение процессов

Предел браузерного процесса 120 с, внутреннего сценария 105 с; UI-ожидания конечны. Сохранены частичный JSON, имя, причина и process.log; runner продолжил остальные случаи и вернул ненулевой результат. Все девять первичных записей отмечают закрытый сервер и завершённый поток. При последней локальной проверке созданных работающих серверов/браузеров не осталось.

Прежний 1B timeout self-test снова PASS: внутренний намеренно зависающий тест TIMEOUT, runner exit 1, процесс -15, работающих PID нет. T06 — пять отдельных node --check до пяти секунд каждый, не единый восьмисекундный тест.

Реальное приложение и рабочий SDK загружались с локального origin; внешний HTTP/Phoenix контролировался fixture без пересылки в production. Только искусственные пользователи и файлы. Service Worker заблокирован; подготовленная выдача неизменённых локальных статических файлов при offline reload не является production offline-shell. Нативные IDB отказы/задержки — тестовая граница, ещё не пройденная сценариями. Production RLS, серверный Auth, физический iPhone и побайтовая сохранность не приняты.

## Документы, внешние записи и остановка

Канонический пакет: `INTEGRATION_1C_MANIFEST.json`, этот `INTEGRATION_1C_REPORT.md`, `INTEGRATION_1C_EVIDENCE.json` в `docs/engineering/block-01/`. Первоисточники и тесты — оригинальные CI ZIP; доступные копии и квитанция фактического HANDOFF_HEAD передаются в скачиваемом пакете. Неопубликованный заблокированный код туда как исправление не включён.

Внешние записи Исполнителя: 15 новых тестовых/CI-файлов, Git-объекты и non-force продвижение рабочей ветки, её CI/артефакты, три документа блокировки. Параллельное дополнение 01 создано не этим исполнением и сохранено без перезаписи. Main при чтении: `f348aceacc3acdb315387a4066f6f495ce269b54`. Main, production, Supabase, RLS, серверный Auth, реальные данные и дизайн не изменены. Merge/deploy, reset/force-push, рассылки и покупки не выполнялись. Принятые 1A/1B и исторические доказательства сохранены.

**Остановка: SUBSTEP_1C_BLOCKED. Готов только отчёт о выполненной части и препятствии, не принятый результат 1C. 1D и блоки 2–5 не начаты.**
